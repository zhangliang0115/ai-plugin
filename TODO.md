现在这个插件具备多个功能，包括 MCP 管理、优化提示词，以后可能还会有其他功能。所以，这个插件本身应该有一个配置页面，每个功能的开启和关闭应该都是可配置，默认配置是可以开启的。像这个优化提示词，现在有点儿冗余，有图标还有文字说明。那这个文字说明也可以默认是隐藏的，否则这个占用的位置可能会比较长，例如这样的应该需要有配置。还有这个优化提示词的规则是什么？它是用的什么模型怎么选择的？然后它仅根据用户输入框进行优化，还是结合用户本次会话这些现在都是怎么做的，是不是也是可以配置化的，还有，是不是生成优化提示词是不是也应有一个提示词是什么的管理，用户可以自定义提示词。点击提示词后应该有一个效果，提示：生成中之类的内容